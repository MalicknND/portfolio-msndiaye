import { en } from "./en";
import { fr } from "./fr";
import { Language, TranslationKeys } from "@/types";

export const translations: Record<Language, TranslationKeys> = {
  en,
  fr,
};

export { en, fr };
